{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ],
    "nohoist": [
      "**/graphql/**"
    ]
  },
  "scripts": {
    "predev": "yarn && cd api && yarn generate",
    "dev": "wsrun dev",
    "test": "wsrun test",
    "lint": "tslint -p tslint.json"
  },
  "devDependencies": {
    "tslint": "^5.12.1",
    "wsrun": "^3.6.4"
  }
}
