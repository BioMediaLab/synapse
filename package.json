{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ],
    "nohoist": [
      "**/graphql/**"
    ]
  },
  "scripts": {
    "predev": "yarn && cd api && yarn generate",
    "dev": "wsrun dev"
  },
  "devDependencies": {
    "wsrun": "^3.3.3"
  }
}
